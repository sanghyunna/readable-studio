// Help launcher — all external promotional links have been removed;
// the component is kept as a null render so call sites compile without change.

export function EntryHelpMenu() {
  return null;
}
